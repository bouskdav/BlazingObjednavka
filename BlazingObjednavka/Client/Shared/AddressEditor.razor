<div class="form-field">
    <label>Jméno:</label>
    <div>
        <InputText @bind-Value="@Address.Name" />
        <ValidationMessage For="@(() => Address.Name)" />
    </div>
</div>

<div class="form-field">
    <label>Adresa:</label>
    <div>
        <InputText @bind-Value="@Address.Line1" />
        <ValidationMessage For="@(() => Address.Line1)" />
    </div>
</div>

<div class="form-field">
    <label>Adresa (2. řádek):</label>
    <div>
        <InputText @bind-Value="@Address.Line2" />
        <ValidationMessage For="@(() => Address.Line2)" />
    </div>
</div>

<div class="form-field">
    <label>Město:</label>
    <div>
        <InputText @bind-Value="@Address.City" />
        <ValidationMessage For="@(() => Address.City)" />
    </div>
</div>

@*<div class="form-field">
    <label>Region:</label>
    <div>
        <InputText @bind-Value="@Address.Region" />
        <ValidationMessage For="@(() => Address.Region)" />
    </div>
</div>*@

<div class="form-field">
    <label>PSČ:</label>
    <div>
        <InputText @bind-Value="@Address.PostalCode" />
        <ValidationMessage For="@(() => Address.PostalCode)" />
    </div>
</div>

<div class="form-field">
    <label>Email:</label>
    <div>
        <InputText @bind-Value="@Address.Email" />
        <ValidationMessage For="@(() => Address.Email)" />
    </div>
</div>

<div class="form-field">
    <label>Telefon:</label>
    <div>
        <InputText @bind-Value="@Address.Phone" />
        <ValidationMessage For="@(() => Address.Phone)" />
    </div>
</div>

@code {
    [Parameter] public BlazingObjednavka.Shared.Address Address { get; set; }
}
